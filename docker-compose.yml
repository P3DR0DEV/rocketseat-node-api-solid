services: 
  database:
    container_name: api-solid-container
    image: bitnami/postgresql:latest
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: api_solid
      PGDATA: /data/postgres
    volumes:
      - ./data/pg:/data/postgres